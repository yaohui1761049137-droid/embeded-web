# /etc/systemd/system/chrony.service.d/restart.conf
# Defensive: chrony previously had no restart policy. The watchdog never
# restarts chrony itself; this only covers a crashed/wedged process.
[Service]
Restart=on-failure
RestartSec=5