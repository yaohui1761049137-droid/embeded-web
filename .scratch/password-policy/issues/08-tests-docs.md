# 08: 测试与文档

- Type: task
- Status: open
- Blocked by: 01, 03, 04, 05, 06, 07

## 任务

1. `test_suite.sh` 新增用例(验收标准 10):
   - 合法密码(四类齐全,10-64 字符)创建/重置/自改成功;
   - 拒绝:长度不足、缺大写/小写/数字/标点、含中文、含空格、与当前密码相同;
   - 到期流程:构造 changed_at=0 的存量用户 → 登录被导向强制改密 → 其他 CGI 被拦截 → 改密后恢复;
   - 踢会话:改密后旧 sid 失效;
   - 7 天预警:构造剩 5 天场景,响应含预警信息。
2. 文档:
   - README.md:安全章节记录密码策略、90 天有效期、存量迁移 SQL(ALTER TABLE)与部署注意事项;
   - QUICKSTART.md:板子存量库升级步骤 + 强制改密流程说明;
   - 明示到期判定依赖板载时钟(无 RTC/NTP 时重启时间错乱可能误判到期,ADR-0002 风险项)。

## 验证

- `./test_suite.sh 192.168.1.100` 全绿(新增用例通过,旧用例不回归);
- README/QUICKSTART 内容与实测行为一致。
